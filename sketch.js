let stars=[];let bullets=[];let enemies=[];let particles=[];let player;let score=0;let gameOver=false;let lastShotFrame=0;function initPlayer(){player={x:200,y:600-40,speed:5,r:14};}function createStars(){for(let i=0;i<30;i++){let s={x:random(0,width),y:random(0,height),r:random(1,3),speed:random(0.5,2)};stars.push(s);} }function updateStars(){for(let i=0;i<stars.length;i++){stars[i].y+=stars[i].speed; if(stars[i].y>height){stars[i].y=random(-20,0); stars[i].x=random(0,width);} } }function drawStars(){noStroke();fill(255);for(let i=0;i<stars.length;i++){circle(stars[i].x,stars[i].y,stars[i].r*2);} }function handleInput(){if(keyIsDown(LEFT_ARROW)){player.x-=player.speed;} if(keyIsDown(RIGHT_ARROW)){player.x+=player.speed;} if(player.x<player.r){player.x=player.r;} if(player.x>width-player.r){player.x=width-player.r;} }function handleShooting(){if(keyIsDown(32)){if(frameCount-lastShotFrame>=8){let b={x:player.x,y:player.y-player.r-4,r:4,speed:8};bullets.push(b);lastShotFrame=frameCount;}}}function updateBullets(){for(let i=bullets.length-1;i>=0;i--){bullets[i].y-=bullets[i].speed; if(bullets[i].y+bullets[i].r<0){bullets.splice(i,1);} } }function drawBullets(){noStroke();fill(50,200,255);for(let i=0;i<bullets.length;i++){circle(bullets[i].x,bullets[i].y,bullets[i].r*2);} }function spawnEnemies(){if(frameCount%60===0){let e={x:random(12,width-12),y:-12,r:12,speed:2};enemies.push(e);} }function updateEnemies(){for(let i=enemies.length-1;i>=0;i--){enemies[i].y+=enemies[i].speed; if(enemies[i].y-enemies[i].r>height){enemies.splice(i,1);} } }function drawEnemies(){noStroke();fill(255,80,80);for(let i=0;i<enemies.length;i++){circle(enemies[i].x,enemies[i].y,enemies[i].r*2);} }function checkCollisions(){for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];let dx=e.x-player.x;let dy=e.y-player.y;let dist2=dx*dx+dy*dy;let pr=player.r+e.r; if(dist2<=pr*pr){gameOver=true;}}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];for(let j=bullets.length-1;j>=0;j--){let b=bullets[j];let dx=e.x-b.x;let dy=e.y-b.y;let dist2=dx*dx+dy*dy;let rr=e.r+b.r; if(dist2<=rr*rr){spawnParticles(e.x,e.y);enemies.splice(i,1);bullets.splice(j,1);score+=10;break;}}}}function spawnParticles(x,y){for(let i=0;i<5;i++){let angle=random(0,Math.PI*2);let speed=random(1,3);let p={x:x,y:y,r:3,life:20,vx:Math.cos(angle)*speed,vy:Math.sin(angle)*speed};particles.push(p);} }function updateParticles(){for(let i=particles.length-1;i>=0;i--){particles[i].x+=particles[i].vx;particles[i].y+=particles[i].vy;particles[i].life--; if(particles[i].life<=0){particles.splice(i,1);} } }function drawParticles(){noStroke();for(let i=0;i<particles.length;i++){let alpha=map(particles[i].life,0,20,0,255);fill(255,200,50,alpha);circle(particles[i].x,particles[i].y,particles[i].r*2);} }function drawPlayer(){noStroke();fill(gameOver?150:100,200,100);triangle(player.x,player.y-player.r,player.x-player.r,player.y+player.r,player.x+player.r,player.y+player.r);}function drawUI(){fill(255);textSize(16);textAlign(LEFT,TOP);text('Score: '+score,10,10);if(gameOver){textSize(32);textAlign(CENTER,CENTER);text('Game Over',width/2,height/2);} }function setup(){createCanvas(400,600);initPlayer();createStars();lastShotFrame=0;score=0;}function draw(){background(0);updateStars();drawStars();if(!gameOver){handleInput();handleShooting();updateBullets();spawnEnemies();updateEnemies();checkCollisions();}updateParticles();drawParticles();drawBullets();drawEnemies();drawPlayer();drawUI();}
